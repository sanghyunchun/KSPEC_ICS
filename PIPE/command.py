import asyncio
import json
from Lib.AMQ import *
import Lib.mkmessage as mkmsg
from .kspec_pipe_controller.{file name} import {class name}


async def identify_execute(PIPE_server, cmd):
    global adcadjust_task
    dict_data = json.loads(cmd)
    func = dict_data['func']


    if func = 'bias':
        function for bias combine?? substract zero images???

        ...
        ...



